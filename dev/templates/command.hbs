// Auto synced from github actions. Don't change this file

import { Bitcoind, Json } from '../../types.js'
import request from '../../rpc-request.js'

type {{typeName}}Params = {
  bitcoind: Bitcoind
{{{rpcParams}}}
}

/**
 * {{{descriptionCommand}}}
 *
 * {{{description}}}
 */
export function {{functionName}}(params: {{typeName}}Params) {
  const { bitcoind, ...methodParams } = params
  return request({ method: '{{rpcMethod}}', params: methodParams }, bitcoind)
}
